/**
 * Central config — change API_URL here when deploying to a different host
 * or when forking the project for another institution.
 */
// Android emulator routes 10.0.2.2 to the host machine's localhost.
// Change this to your PC's local IP (e.g. 192.168.x.x) for a physical device.
export const API_URL = 'http://10.0.2.2:3000';
