/**
 * API_URL points to the local json-server when running on an Android emulator
 * (10.0.2.2 routes to the host machine's localhost).
 *
 * To use the public fallback instead (no local server needed), change to:
 * https://my-json-server.typicode.com/bvc-mobile-dev/dev-finder
 *
 * For a physical device on the same Wi-Fi, use your PC's local IP:
 * http://192.168.x.x:3000
 */
export const API_URL = 'http://10.0.2.2:3000';
